apiVersion: v1
kind: Service
metadata:
  name: {{ include "ldap-manager.fullname" . }}
  labels:
    {{- include "ldap-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ldap-manager.selectorLabels" . | nindent 4 }}
