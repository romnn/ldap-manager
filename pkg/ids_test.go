package pkg

import (
	"testing"

	// "github.com/romnn/deepequal"
	// pb "github.com/romnn/ldap-manager/pkg/grpc/gen"
)

// TestGetHighestGID tests getting the highest GID
func TestTestGetHighestGID(t *testing.T) {
	test := new(Test).Setup(t)
	defer test.Teardown()

}

// TestGetHighestUID tests getting the highest UID
func TestTestGetHighestUID(t *testing.T) {
	test := new(Test).Setup(t)
	defer test.Teardown()

}
